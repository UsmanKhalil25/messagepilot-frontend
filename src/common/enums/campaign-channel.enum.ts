export enum CampaignChannel {
  EMAIL = "email",
  SMS = "sms",
  WHATSAPP = "whatsapp",
  SLACK = "slack",
  DISCORD = "discord",
}

export const campaignChannelValues = Object.values(CampaignChannel);
